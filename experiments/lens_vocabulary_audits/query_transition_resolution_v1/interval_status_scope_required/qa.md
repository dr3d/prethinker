# Interval Status Scope Required QA

Questions only. Reference answers are isolated in `oracle.jsonl`.

1. When did temporary key TK-31 become effective?
2. When did TK-31's active interval end?
3. During the active interval, what status and scope applied to TK-31?
4. What status applied after the active interval ended?
5. When was the renewal packet due?
