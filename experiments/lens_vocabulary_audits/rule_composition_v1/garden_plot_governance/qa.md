# Garden Plot Governance QA

Questions only. Reference answers are isolated in `oracle.jsonl`.

1. What is the base rule for plot transfers?
2. What eligibility condition must a recipient satisfy for an ordinary transfer?
3. What threshold defines a hardship transfer?
4. What exception allows transfer with only one workday attended?
5. What vote is required when a transfer bypasses the workday condition?
6. Which rule overrides the hardship exception in a conflict?
7. Which rule has precedence over every transfer exception?
8. What fallback rule applies if the monthly meeting lacks quorum?
