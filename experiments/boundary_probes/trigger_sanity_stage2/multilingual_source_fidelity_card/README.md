# Multilingual Source Fidelity Card

Pressure:

The source states count, inclusion, exclusion, approval, and withdrawn-estimate
distinctions in non-English prose with English QA. The compiler may recognize
the distinctions in planning metadata while failing to emit queryable
coordinates.

Expected boundary class:

`non_english_compile_surface_omission`
