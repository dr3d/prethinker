# Canonical Alias Register

Pressure:

The same entity is named once as a bare tag and later as a noun-prefixed tag.
The compiler should preserve one canonical entity per source item rather than
creating a second entity from the noun phrase.

Expected boundary class:

`canonical_identifier_duplication`
