# Canonical Alias Register QA

Questions only. Reference answers are isolated in `oracle.jsonl`.

1. How many tags are in the review set?
2. Which review-set tags are ready?
3. How many review-set tags are ready?
4. Which review-set tag is paused?
5. Which review-set tag is retired?
6. Does archive tag BX-20 count as an available review sample?
7. How many available review samples are there?
8. Does the word "sample" before AX-10 create a second review-set item?
