# State Word Register

Trigger audit target: predicate-name convention.

The document describes item condition/state without using a visible `status`
heading. The question is whether source compilation and query-time helpers can
still expose scoped state counts without relying only on `*_status` names.
