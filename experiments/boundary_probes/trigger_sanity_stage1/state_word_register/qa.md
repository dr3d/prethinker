# State Word Register QA

Questions only. Reference answers are isolated in `oracle.jsonl`.

1. How many units are in the active shelf group?
2. Which active shelf group units are usable?
3. How many active shelf group units are usable?
4. Which active shelf group units are held for inspection?
5. How many active shelf group units are held for inspection?
6. Does archive unit Z-01 count as an available active unit?
7. Which unit in the active shelf group is retired?
8. How many available active units are there?
