# Authority Envelope Resolution

Purpose: perturb an apparently easy authority/role interior with decoy role
surfaces. Exactness should not depend on the absence of nearby recorder,
reviewer, transmitter, and governing-authority rows.

Forbidden fix: do not special-case this control file, role labels, person names,
or answer strings. The probe asks whether authority roles remain distinct as a
generic compile/query coordinate.
