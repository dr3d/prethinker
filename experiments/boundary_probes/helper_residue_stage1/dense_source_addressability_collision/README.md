# Dense Source Addressability Collision Probe

Purpose: dense unlike probe for no-helper source-addressability pressure.

This fixture adds density that the clean probes did not contain:

- multiple nearby sections with similar temporal labels,
- repeated role-bearing signature lines,
- more than one basis/corroboration pair,
- object-title language near legal/ownership title language,
- equipment vendor/model rows near service and calibration rows.

The target is not any local name in this probe. The target is whether the
compile and QA planner preserve and select the right source-addressable
coordinate when adjacent coordinates are plausible but wrong.
