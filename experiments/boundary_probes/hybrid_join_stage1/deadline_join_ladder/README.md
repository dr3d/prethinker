# Deadline Join Ladder

Purpose: isolate a hybrid-join boundary where the answer requires combining a
base rule, a tolled interval, and a granted extension while rejecting a decoy
withdrawn request.

Forbidden fix: do not encode this permit id, dates, or extension labels in
harness logic. Any repair must be a generic temporal/rule/input join.
